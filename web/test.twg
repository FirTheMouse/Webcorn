##
    register root_path;
    "root_path" {
        in htmlemiting {
            emit_contents;
        }
    }

    MessageData {
        string sent_by;
        int sent_date;
        string return_path;
        string comments;
    }

##

root_path {
    script read_file("web/webcorn.js");
    div {
        "display" : "flex";
        "flex-direction" : "row";
        button {
            text "RESET";
            "onclick" : "resetWebrunner();";
        }
            button {
            text "DUMP";
            "onclick" : capture{dump_unit(true);};
        }
    }
    text "Hello World";
}
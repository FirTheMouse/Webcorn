while(0<1) {
    if(unit_index!=0) {
        string url = unit_label;
        string page = http_get(url, "/");
        string rr = "REPORT:"+page;
        cry(rr);
        unit_setindex(0);
    } else {
        thread_sleep;
    }
}








                                